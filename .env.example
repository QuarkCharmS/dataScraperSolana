# Solana RPC endpoint (optional - defaults to public endpoint)
# Get a free RPC endpoint from providers like Helius, QuickNode, or Alchemy for better performance
RPC_ENDPOINT=https://api.mainnet-beta.solana.com

# Solana WebSocket endpoint (optional)
RPC_WEBSOCKET_ENDPOINT=wss://api.mainnet-beta.solana.com

# Shyft API key (required for liquidity data)
# Get your free API key at: https://shyft.to/
SHYFT_API_KEY=your_shyft_api_key_here

# WebSocket server address (optional - defaults to localhost)
SOCKET_ADDRESS=localhost
